/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  allowedDevOrigins: [
    'prefamously-unsealed-suzan.ngrok-free.dev',
    '*.ngrok-free.dev',
    '*.ngrok.io'
  ]
};

export default nextConfig;

